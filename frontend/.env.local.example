# API URL for local development (without Docker)
# NEXT_PUBLIC_API_URL=http://localhost:8080

# API URL for Docker development (set automatically in docker-compose.yml)
# NEXT_PUBLIC_API_URL=http://backend:8080
